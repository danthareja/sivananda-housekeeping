type Room {
  id: Int!
  name: String!
  lodgingId: Int!
  lodgingName: String!
  location: String!
  cleaningTime: Int!
  cartCost: Int!
  dirty: Boolean!
  givenKey: Boolean!
  cleanedAt: String
  housekeeper: String
  order: Int
  comments: [String!]!
  arrivals: [ArrivingGuest!]!
  departures: [DepartingGuest!]!
}
