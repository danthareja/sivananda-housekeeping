type Reservation {
  key: String! @unique # "${roomId}:${date}" ex. "97:2018-10-31"

  cleaned: Boolean!
  cleanedAt: DateTime

  givenKey: Boolean!
  givenKeyAt: DateTime

  housekeeper: String
  priority: Int
  comments: [String!]!
}